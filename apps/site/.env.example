# Canonical URL of this marketing site (metadataBase, canonicals, sitemap, robots).
NEXT_PUBLIC_SITE_URL=http://localhost:3003

# Base URL of the investor web app — header "Sign in" / "Get started" link here.
NEXT_PUBLIC_WEB_URL=http://localhost:3000
